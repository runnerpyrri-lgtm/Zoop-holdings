# 코덱스 대기 요청 목록 (Codex-pending queue)

> 회장 지시(2026-07-20): 코덱스 토큰이 **일주일 뒤** 들어온다. 그 전까지 앱 저장소(야외봄·청약봄·러닝봄·
> 캘린더봄·자격증봄·노트봄) **코드 수정이 필요한 일**은 여기에 적어 두고, 토큰이 오면 코덱스가 각 앱
> **원본 저장소에서** 실행한다(CLAUDE.md: 앱 코드는 해당 앱 원본 저장소에서만 수정).
>
> 그 사이 **HQ 본체(robom 저장소)** 개선은 Claude가 직접 계속한다(이 저장소 내이므로 코덱스 불필요).
>
> 원칙: 거짓 성과 금지 — 실제 결함·명확한 개선만. 각 항목은 목표·합격기준·앱 저장소를 명시한다.

## 지금 즉시(HIGH) — 코덱스 연결 시 최우선

없음(현재 HQ health 엔진이 실제 확정 장애를 감지하면 이 목록 위가 아니라 **결재함**으로 올라간다).
프로덕션 실측 장애는 HQ 실행 중 자동 상신되므로, 아래는 **성장·리팩터 백로그**(급한 장애 아님)다.

## 앱별 성장 백로그(MED) — ROADMAP 계승, 코덱스 실행 대상

### 야외봄 (robom-labs/outbom)
- [ ] 대형 화면 파일 분리(단일 거대 컴포넌트 → 화면/기능 단위 모듈). 합격: 최대 파일 라인수 하락 + 빌드·테스트 그대로 통과.
- [ ] 날씨·대기질 추천 로직 단위 테스트 추가(골든 케이스). 합격: 추천 함수 입력→출력 고정 테스트 존재.
- [ ] 백그라운드 알림 안정화(권한·재시도·중복 억제). 합격: 알림 스케줄 로직에 재시도·dedup 테스트.

### 청약봄 (robom-labs/homebom)
- [x] core와 Edge Function의 공고 정규화 로직 **단일 소스화**(중복 제거). 합격: 정규화 코드 1곳, 양쪽이 import.
  → Claude가 코덱스 토큰 부재 기간 대행 실행(2026-07-20). [homebom#32](https://github.com/robom-labs/homebom/pull/32)(draft). 정규화 전후 픽스처 대조로 legacyIds 잠재 버그도 함께 발견·수정. **병합 전 Supabase 배포 스모크 테스트 필수**(PR 본문에 명시 — 로컬 deno check만으로는 실제 배포 번들링 검증 불가).
- [ ] Web Push RFC 상세 설계 반영(방문→알림 허용 흐름). 합격: 구독 등록·해제·전송 경로 테스트.

### 러닝봄 (robom-labs/runningbom)
- [ ] 대회 데이터 갱신 자동화(스케줄 수집 + fingerprint 대조). 합격: 수집 실패/무변경/변경을 구분해 기록.
- [x] 외부 접수 링크 상태 점검 강화(404·리다이렉트 감지). 합격: 링크 헬스체크가 죽은 링크를 잡는 테스트.
  → Claude가 코덱스 토큰 부재 기간 대행 실행(2026-07-20). [runningbom#34](https://github.com/robom-labs/runningbom/pull/34)(draft). CI 워크플로 배선은 후속(배포 경로 변경은 검증 없이 하지 않음).

### 공통(6앱)
- [ ] 앱별 **최소 사용 분석** 도구 준비(개인정보 최소 수집, 비공개 endpoint). 합격: 수집 항목·보관 정책 문서 + opt 경로.
- [ ] 방문→알림 허용 **전환율 측정** 지표. 합격: 결정론적 계산 함수 + 테스트.
- [ ] 네 저장소 branch protection·CODEOWNERS 정리(사람 권한 필요 — HUMAN-TASKS와 병행).

## 사람(회장)만 가능 — 코덱스도 못 함 (HUMAN-TASKS.md 참조, 여기 요약)
- Apple/Google 개발자 계약·Bundle/App ID·서명 지문·심사 제출.
- Q-Net `QNET_SERVICE_KEY`, Kakao/Google/Apple OAuth 등록, 분석 endpoint credential.
- 결제·계정·비밀값·법률 검토.
> 이 범주는 코덱스에게도 넘기지 않는다(승인 경계).

## 진행 방식
1. 코덱스 토큰 도착 시: 위 MED 항목을 **한 번에 하나씩** 해당 앱 저장소에서 Loop(목표→기준→수정→원래계약 재검증→종료)로 실행.
2. 각 항목 완료 시 이 파일에서 `[x]`로 체크하고 결과(PR·검증)를 한 줄 남긴다.
3. HQ 본체 개선(robom 저장소)은 코덱스와 무관하게 Claude가 계속 audit→fix→release.

## 회장 결재함 실물 항목 — 코덱스 부재 기간 Claude 직접 대행 (2026-07-20)

회장이 HQ 앱 결재함 스크린샷으로 직접 전달한 항목 중, 사람 확인·비밀값·계정이 필요 없고 즉시 검증 가능한
것은 대기열에 적재하지 않고 Claude가 그 자리에서 처리한다. 각 앱 코드는 CLAUDE.md 원칙대로 해당 앱
원본 저장소에서만 수정한다(로컬 브랜치+테스트까지 완료, main push는 draft PR 또는 회장 재가 확인 후).

- **캘린더봄 — "70대 실제 사용자 과업 테스트"(문서, 이미 회장 재가 완료 7.19 15:37)**: 완료.
  [calendarbom#4](https://github.com/robom-labs/calendarbom/pull/4)(draft) — 달력 이전/다음달 버튼을 앱 자체 48px 고령자 기준으로 확대.
- **야외봄 — "다음 개선·신기능 발굴"(문서 제4771호, 7.20 상신, 재가 대기 중)**: 배경 에이전트가
  `/workspace/outbom`에서 감사·구현까지 로컬로 완료(오늘 탭 리렌더 최적화, React.memo·useCallback, 최신 main 위에 rebase까지 마침). 아직 회장 미재가라 push·PR은 보류, 재가 확인 후 즉시 처리 가능.
- **자격증봄 — 고령자 글자 크기 접근성 버그**: Claude가 코덱스 부재 기간 감사·대행(2026-07-20).
  [certbom#14](https://github.com/robom-labs/certbom/pull/14)(draft) — 저장된 글자 크기(`certbom-font-scale`)가
  설정 화면 진입 시에만 적용돼, 재실행하면 130% 설정이 무시되고 100%로 표시되던 것을 `main.tsx` 시작 시
  복원으로 수정(단일 출처 `font-scale.ts` 헬퍼). 저장키·스키마·비밀값(QNET) 불변, 테스트 50→54.
- **노트봄 — ASR 엔진 정직 표시 버그**: Claude가 코덱스 부재 기간 감사·대행(2026-07-20).
  [notebom#11](https://github.com/robom-labs/notebom/pull/11)(draft) — whisper.worker의 `usedFallback` 모듈 전역 플래그가
  리셋 안 돼, 1차 실패 후 재시도에서 고정확도 모델이 성공해도 "경량 모델(낮은 정확도)"로 오표시하던 것을
  `createEngineTruth()` 헬퍼+파이프라인 재빌드 시 reset으로 수정. 저장키·스키마 불변, 테스트 65→68.
- **청약봄 — "워크플로가 죽으면 수집이 멈춥니다"(문서 제4899호, 27시간 오경보)**: 앱 코드 문제가 아니라
  HQ 쪽 계약 설계 결함(GitHub Actions 최근 run 나이를 수집 파이프라인 대리 신호로 오용 — 실제 수집은
  Supabase pg_cron)으로 확인돼, robom 저장소에서 직접 수정·릴리스(hq-v3.3.27). 앱 대기열에는 추가하지 않음.
- **청약봄 — 공급면적 타일 글자 화면 밖 잘림(회장 스크린샷 제보 7.20)**: Claude가 감사·대행(2026-07-20).
  [homebom#33](https://github.com/robom-labs/homebom/pull/33)(draft, CI green) — 상세 카드 공급면적 범위값
  (예: `120.81~227.13㎡`)이 단일 `detail__nowrap` 한 덩어리라 3열 좁은 타일(≈390px)에서 카드
  `overflow:hidden` 밖으로 잘렸다. 분양가 타일처럼 각 숫자는 nowrap으로 붙이되 물결(`~`) 앞에 `<wbr/>`을
  넣어 좁을 때만 줄바꿈되게 수정. 실제 Chromium 390px 렌더로 잘림(101px>82px)→해소 시각 검증. 저장키·
  버전·family.lock 불변, 테스트 37→40.
- **6앱 오버플로 전수 시각 점검(회장 지시 "니가 실제로 미리보기처럼 어플마다 화면 띄워서 다 해결")**:
  완료. 웹 렌더 가능한 앱(청약봄·캘린더봄·노트봄·자격증봄)을 실제 Chromium 360/390px로 띄워 DOM 계측
  (scrollWidth>clientWidth, rect.right>innerWidth). **청약봄**=위 homebom#33 수정. **노트봄**=긴 URL·영문
  토큰이 노트카드 제목·미리보기·인식 원문에서 잘림 → [notebom#12](https://github.com/robom-labs/notebom/pull/12)
  (overflow-wrap:anywhere, 테스트 65). **자격증봄·캘린더봄**=실측 결과 오버플로 없음(정상, 가짜 수정 안 함).
  야외봄·러닝봄 모바일은 RN이라 DOM 렌더 불가 — 정직 표기.
- **야외봄 준비물 물 중복·'전해질' 표현(회장 스크린샷 제보 7.20 — "러닝봄"으로 지칭했으나 실제는 야외봄
  준비 탭의 러닝 활동 준비물)**: [outbom#28](https://github.com/robom-labs/outbom/pull/28)(draft). 물이 필수
  (추가 물·간식)+날씨(물·전해질) 두세 곳에 겹쳐 뜨던 것을 한 항목으로 통합, '전해질'→'이온음료(포카리·게토레이
  등)', 간식은 물과 분리, "챙긴 건 눌러서 표시" 친근 안내 추가. 실제 렌더로 물 1개 확인, 테스트 122.
- **자격증봄 시험 데이터 확충(회장 지시 "97개가 다인지 사이트 뒤져 늘리되 크로스체크·허위 금지")**:
  [certbom#15](https://github.com/robom-labs/certbom/pull/15)(draft). 기존 100종목 이름 전수 대조로 중복
  배제(컴활·워드·사회조사·직업상담은 이미 있어 제외), Q-Net jmCd로 실재 확인한 7종만 추가(전기기능사 7780·
  자동차정비기능사 6281·용접기능사 6222·조경기사 1370·품질경영기사 1500·정보처리기능사·컴퓨터그래픽스운용기능사).
  접수 일정은 확인 못 해 events 비움(허위 일정 금지). 97→104, id·이름 중복 0, 테스트 통과.
- **캘린더봄 일정 생성 UX 재설계(회장 지시 "눌렀을 때 와닿게·뒤 캘린더 헷갈림·시간입력 구글/네이버급·플로
  처음부터")**: 진행 중(배경 에이전트가 스크림 강화·탭 피드백·시간 선택기 재설계·플로 정리 구현, 실제 렌더
  스크린샷 검증 후 로컬 커밋). 검증 후 push+Draft PR 예정.
