READ_COMPLETE — NOTEBOM_CLAUDE_CODE_FULL_HANDOFF_v1.0.0

- MANIFEST.sha256 verified: 52/52 OK
- All mandatory docs read: 00,01(770 lines full),02,03,04,05,06,07, README, MANIFEST.json
- All research raw-results read: ALL_METRICS.csv, ARTIFACT_INDEX.json, every REPORT.md + summary.json + qwen failure log
- Research metrics RECOMPUTED from raw summary.json and confirmed consistent with 02_VERIFIED_RESEARCH_RESULTS.md:
    * Whisper Turbo auto (n=8): FLEURS clean CER 0.0076 / HiKE clean CER 0.1996 / EN recall .77-.81 / halluc 0 -> best balanced final candidate
    * Turbo ko (n=8): HiKE clean CER 0.3287, halluc 0.125, EN recall 0.59 -> ko-forced loses English on code-switch
    * SenseVoice int8 (n=8): RTF ~0.07 but HiKE ~65% / EN ~52% -> quick-draft only
    * Ghost KO fine-tune: zeroth 99% but independent FLEURS 89% / HiKE 23-46% / EN ~0 -> corpus overfit, reject
    * Qwen3-0.6B failure = loader KeyError 'qwen3_asr' (Transformers arch unrecognized), NOT a quality failure
- read-ledger.json written (39 text files, all read_complete=true)

Read gate CLOSED. Code changes permitted from here, subject to:
  - GitHub latest main/PR state overrides this snapshot (verify next)
  - Company rule: app code edited only in robom-labs/notebom, via Draft PR
  - No unmeasured accuracy numbers; real-device claims = BLOCKED_EXTERNAL until verified on hardware
